/** Initializes a new Square game and starts running it. */
class Main {
    function void main() {
        var DinoGame game;
        let game = DinoGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
